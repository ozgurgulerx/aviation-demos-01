apiVersion: v1
kind: ServiceAccount
metadata:
  name: aviation-rag-backend
  namespace: ${K8S_NAMESPACE}
  annotations:
    azure.workload.identity/client-id: "${FABRIC_WORKLOAD_IDENTITY_CLIENT_ID}"
  labels:
    azure.workload.identity/use: "true"
